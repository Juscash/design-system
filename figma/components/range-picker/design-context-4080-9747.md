# Figma — Range Picker (`4080:9747`) — get_design_context

> ATENÇÃO: o nó `4080:9747` retornou apenas o `Page header` do bloco Avatar (icon/users + título "Avatar" + descrição "Componente com foto ou iniciais para representar o usuário."). Sugere que esse ID foi reaproveitado ou que o componente real RangePicker foi removido/movido na atualização do Figma. O componente atual de range de datas vive dentro do DatePicker (4066:8379), com `state=placeholder/value/focus, variant=range` no nó `4115:12926` (`date picker input`).

```tsx
const imgEehd9Xsz19K1 = "../../assets/img-logo-juscash-white.png";
const imgVectorStroke = "../../assets/img-icon-stroke-light-37x33.svg";

export default function PageHeader() {
  return (
    <div data-node-id="4080:9747" data-name="Page header">
      {/* icon/users + "Avatar" + "Componente com foto ou iniciais para representar o usuário." */}
    </div>
  );
}
```

Styles: heading/02 49px, heading/06 20px.
